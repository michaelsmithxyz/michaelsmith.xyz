name: "Commit checks"
on: [push]
jobs:
  typecheck:
    name: 'Typecheck smith.pizza'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x
      - run: make smith.pizza/check